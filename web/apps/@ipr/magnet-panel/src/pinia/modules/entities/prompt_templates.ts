import entityFactory from '@/pinia/utils/entityFactory'

const usePromptTemplates = entityFactory('prompt_templates', {
  state: {},
  getters: {},
  actions: {},
})

export default usePromptTemplates

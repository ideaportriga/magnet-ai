# External Plugins Deployment Strategies - Visual Guide

## 🎯 Три стратегии развертывания

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    STRATEGY 1: SEPARATE REPOSITORIES                         │
│                           ⭐ RECOMMENDED ⭐                                   │
└─────────────────────────────────────────────────────────────────────────────┘

GitHub:
┌─────────────────────────────────┐
│ your-org/magnet-ai (PUBLIC)     │
│ ├── api/                        │
│ │   └── src/                    │
│ │       └── plugins/            │
│ │           ├── builtin/        │ ✅ All public plugins
│ │           │   └── knowledge_  │
│ │           │       source/     │
│ │           └── external/       │ ⚠️ Empty (only __init__.py)
│ └── docs/                       │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ your-org/magnet-plugins-file    │
│              (PRIVATE)          │
│ └── magnet_plugins_file/        │
│     ├── __init__.py             │ ✅ Auto-registration
│     └── plugin.py               │ ✅ Client A specific code
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ your-org/magnet-plugins-        │
│          fluidtopics (PRIVATE)  │
│ └── magnet_plugins_fluidtopics/ │
│     ├── __init__.py             │ ✅ Auto-registration
│     └── plugin.py               │ ✅ Client B specific code
└─────────────────────────────────┘

Deployment:
┌──────────────────────────────────────────────────────────────┐
│ Dockerfile                                                    │
│ ─────────────────────────────────────────────────────────   │
│ FROM python:3.12                                             │
│                                                              │
│ # Install main app                                           │
│ COPY api/ /app/                                              │
│ RUN pip install -e /app                                      │
│                                                              │
│ # Install client-specific plugins from private repos        │
│ ARG GITHUB_TOKEN                                             │
│ RUN pip install git+https://${GITHUB_TOKEN}@github.com/     │
│     your-org/magnet-plugins-file.git@v1.0.0                  │
│                                                              │
│ ENV MAGNET_PLUGINS=magnet_plugins_file.plugin                │
│ CMD ["python", "run.py"]                                     │
└──────────────────────────────────────────────────────────────┘

PROS:
✅ Complete code isolation
✅ Per-client access control  
✅ Independent versioning (git tags)
✅ CI/CD ready
✅ No risk of leaking client code

CONS:
⚠️ Need to create separate repositories
⚠️ Slightly more setup complexity

USE WHEN:
🎯 Publishing main code to public GitHub
🎯 Working with multiple clients
🎯 Need strict access control
🎯 Production deployments


┌─────────────────────────────────────────────────────────────────────────────┐
│                    STRATEGY 2: LOCAL DIRECTORY                               │
│                    (Development & Private Deployments)                       │
└─────────────────────────────────────────────────────────────────────────────┘

GitHub:
┌─────────────────────────────────┐
│ your-org/magnet-ai              │
│ ├── api/                        │
│ │   └── src/                    │
│ │       └── plugins/            │
│ │           ├── builtin/        │ ✅ In git
│ │           │   └── knowledge_  │
│ │           │       source/     │
│ │           └── external/       │
│ │               ├── __init__.py │ ✅ In git
│ │               └── knowledge_  │
│ │                   source/     │
│ │                   ├── __init. │ ✅ In git
│ │                   ├── file.py │ ❌ In .gitignore
│ │                   └── fluid.py│ ❌ In .gitignore
└─────────────────────────────────┘

.gitignore:
┌──────────────────────────────────────────────────────────────┐
│ # Exclude all external plugins                               │
│ src/plugins/external/*/                                      │
│                                                              │
│ # But keep structure                                         │
│ !src/plugins/external/*/__init__.py                          │
│ !src/plugins/external/__init__.py                            │
└──────────────────────────────────────────────────────────────┘

Development:
┌──────────────────────────────────────────────────────────────┐
│ # Just place plugin files - they load automatically!         │
│ $ vim api/src/plugins/external/knowledge_source/file.py      │
│ $ python api/run.py                                          │
│                                                              │
│ No env vars needed! ✅                                       │
└──────────────────────────────────────────────────────────────┘

Deployment:
┌──────────────────────────────────────────────────────────────┐
│ Option A: Volume Mount                                       │
│ ─────────────────────────────────────────────────────────   │
│ docker-compose.yml:                                          │
│   volumes:                                                   │
│     - /secure/client-a-plugins:/app/src/plugins/external/    │
│         knowledge_source                                     │
│                                                              │
│ Option B: Copy during build                                  │
│ ─────────────────────────────────────────────────────────   │
│ Dockerfile:                                                  │
│   ARG CLIENT_NAME                                            │
│   COPY ${CLIENT_NAME}-plugins/* /app/src/plugins/external/   │
│       knowledge_source/                                      │
└──────────────────────────────────────────────────────────────┘

PROS:
✅ Auto-loading (no env vars)
✅ Simple development workflow
✅ No extra repositories

CONS:
⚠️ Need external delivery mechanism
⚠️ Risk of accidental commit (mitigated by .gitignore)
⚠️ Not suitable for public GitHub

USE WHEN:
🎯 Development environment
🎯 Private internal deployments
🎯 Single client scenario
🎯 Temporary solution


┌─────────────────────────────────────────────────────────────────────────────┐
│                    STRATEGY 3: GIT SUBMODULES                                │
│                    (For Experienced Teams)                                   │
└─────────────────────────────────────────────────────────────────────────────┘

GitHub Structure:
┌─────────────────────────────────┐
│ your-org/magnet-ai              │
│ ├── api/                        │
│ │   └── src/                    │
│ │       └── plugins/            │
│ │           ├── builtin/        │ ✅ In main repo git
│ │           └── external/       │
│ │               └── client_a/   │ ⚠️ Git submodule
│ │                   └── know.../ │   → private repo
│ │                       └── x.py│
└─────────────────────────────────┘
                    ↓ submodule reference
┌─────────────────────────────────┐
│ your-org/magnet-plugins-client-a│
│              (PRIVATE)          │
│ └── knowledge_source/           │
│     └── custom.py               │
└─────────────────────────────────┘

.gitignore:
┌──────────────────────────────────────────────────────────────┐
│ # Ignore submodule directory                                 │
│ src/plugins/external/client_a/                               │
└──────────────────────────────────────────────────────────────┘

Setup:
┌──────────────────────────────────────────────────────────────┐
│ # Add submodule                                              │
│ $ git submodule add https://github.com/org/magnet-plugins-   │
│       client-a.git api/src/plugins/external/client_a         │
│                                                              │
│ # Clone for client                                           │
│ $ git clone https://github.com/org/magnet-ai.git             │
│ $ git submodule init                                         │
│ $ git submodule update --init --recursive                    │
│     api/src/plugins/external/client_a                        │
└──────────────────────────────────────────────────────────────┘

PROS:
✅ All in one place during development
✅ Version tracking (submodule commit ref)
✅ Easy to switch plugin versions

CONS:
⚠️ Git complexity (submodules are tricky)
⚠️ Easy to accidentally commit submodule ref
⚠️ Requires team git expertise

USE WHEN:
🎯 Team is experienced with git
🎯 Need tight integration
🎯 Want to version-lock main app ↔ plugin


═══════════════════════════════════════════════════════════════════════════════
                          COMPARISON TABLE
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────┬──────────────┬──────────────┬──────────────┐
│    Criteria        │  Separate    │    Local     │      Git     │
│                    │   Repos ⭐   │  Directory   │  Submodules  │
├────────────────────┼──────────────┼──────────────┼──────────────┤
│ Code Isolation     │  ⭐⭐⭐⭐⭐  │    ⭐⭐      │   ⭐⭐⭐⭐   │
│ Setup Complexity   │    ⭐⭐⭐⭐  │  ⭐⭐⭐⭐⭐  │     ⭐⭐     │
│ Access Control     │  ⭐⭐⭐⭐⭐  │      ⭐      │   ⭐⭐⭐⭐   │
│ CI/CD Integration  │  ⭐⭐⭐⭐⭐  │    ⭐⭐⭐    │    ⭐⭐⭐    │
│ Version Management │  ⭐⭐⭐⭐⭐  │     ⭐⭐     │   ⭐⭐⭐⭐   │
│ Development UX     │    ⭐⭐⭐    │   ⭐⭐⭐⭐   │  ⭐⭐⭐⭐⭐  │
│ Leak Risk         │  ⭐⭐⭐⭐⭐  │      ⭐      │    ⭐⭐⭐    │
│ Multi-Client      │  ⭐⭐⭐⭐⭐  │    ⭐⭐⭐    │    ⭐⭐⭐    │
└────────────────────┴──────────────┴──────────────┴──────────────┘


═══════════════════════════════════════════════════════════════════════════════
                        DECISION TREE
═══════════════════════════════════════════════════════════════════════════════

                    Publishing to GitHub?
                           │
              ┌────────────┴────────────┐
              │                         │
             YES                       NO
              │                         │
              ↓                         ↓
     ┌────────────────┐      Single client or internal?
     │ Multiple       │                │
     │ clients?       │       ┌────────┴────────┐
     └───┬────────────┘       │                 │
         │                  YES               NO
    ┌────┴────┐               │                 │
   YES       NO               ↓                 ↓
    │         │      ┌─────────────────┐  ┌─────────────┐
    │         │      │ LOCAL DIRECTORY │  │  SEPARATE   │
    ↓         ↓      │   (Development) │  │    REPOS    │
┌─────────┐ ┌─────┐ └─────────────────┘  └─────────────┘
│SEPARATE │ │LOCAL│
│  REPOS  │ │ DIR │
│    ⭐   │ │  or │
│         │ │ GIT │
│         │ │ SUB │
└─────────┘ └─────┘


═══════════════════════════════════════════════════════════════════════════════
                    REAL-WORLD EXAMPLES
═══════════════════════════════════════════════════════════════════════════════

Example 1: Open Source Project with Enterprise Clients
───────────────────────────────────────────────────────
Scenario: You want to publish magnet-ai on GitHub, but have custom
         plugins for Client A (banking) and Client B (healthcare)

Solution: SEPARATE REPOS ⭐

Public:  github.com/your-org/magnet-ai
         └── Contains all builtin plugins

Private: github.com/your-org/magnet-plugins-banking
         └── Banking-specific data sources
         
Private: github.com/your-org/magnet-plugins-healthcare  
         └── HIPAA-compliant healthcare integrations

Deploy for Client A:
  docker build --build-arg GITHUB_TOKEN=$TOKEN \
               --build-arg PLUGINS=banking

Deploy for Client B:
  docker build --build-arg GITHUB_TOKEN=$TOKEN \
               --build-arg PLUGINS=healthcare


Example 2: Internal Company Tool
─────────────────────────────────
Scenario: Internal tool, not publishing anywhere, single deployment

Solution: LOCAL DIRECTORY

Repo:    internal-gitlab.company.com/team/magnet-ai (PRIVATE)
         └── plugins/external/  (with real plugin files)

Deploy:  Simple docker build, no extra steps needed


Example 3: SaaS with Per-Customer Customizations
─────────────────────────────────────────────────
Scenario: Multi-tenant SaaS, each customer has custom integrations

Solution: SEPARATE REPOS + Dynamic Loading

Public:  github.com/company/magnet-saas
Private: github.com/company/magnet-plugins-customer-*

Config:  
  customer-a.env:
    MAGNET_PLUGINS=magnet_plugins_customer_a.integration
    
  customer-b.env:
    MAGNET_PLUGINS=magnet_plugins_customer_b.legacy_system,
                   magnet_plugins_customer_b.crm


═══════════════════════════════════════════════════════════════════════════════
                        MIGRATION PATH
═══════════════════════════════════════════════════════════════════════════════

Current State → Target State

┌───────────────────────────────────────────────────────────────┐
│ NOW: Development                                              │
│ ────                                                          │
│ Everything in one repo:                                       │
│   plugins/external/knowledge_source/file.py                   │
│   plugins/external/knowledge_source/fluidtopics.py            │
│                                                               │
│ ✅ Auto-loading works                                        │
│ ✅ Easy development                                          │
│ ⚠️ Not ready for public GitHub                              │
└───────────────────────────────────────────────────────────────┘
                            ↓
                    Migration Steps
                            ↓
┌───────────────────────────────────────────────────────────────┐
│ STEP 1: Update .gitignore ✅ DONE                            │
│ ────────────────────────────────────────────────────────────│
│ src/plugins/external/*/                                       │
│ !src/plugins/external/*/__init__.py                           │
└───────────────────────────────────────────────────────────────┘
                            ↓
┌───────────────────────────────────────────────────────────────┐
│ STEP 2: Remove from git (if already committed)                │
│ ────────────────────────────────────────────────────────────│
│ $ git rm --cached src/plugins/external/knowledge_source/*.py  │
│ $ git commit -m "Remove client-specific plugins"             │
└───────────────────────────────────────────────────────────────┘
                            ↓
┌───────────────────────────────────────────────────────────────┐
│ STEP 3: Create separate repos (optional, for production)      │
│ ────────────────────────────────────────────────────────────│
│ $ mkdir magnet-plugins-file                                   │
│ $ cd magnet-plugins-file                                      │
│ $ # Copy plugin code                                          │
│ $ git init && gh repo create --private                        │
└───────────────────────────────────────────────────────────────┘
                            ↓
┌───────────────────────────────────────────────────────────────┐
│ FINAL: Ready for GitHub! ✅                                  │
│ ────────────────────────────────────────────────────────────│
│ Development:  Keep local files in external/                   │
│ Production:   Install from private repos                      │
│ Public repo:  Contains only builtin plugins                   │
└───────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                    QUICK REFERENCE
═══════════════════════════════════════════════════════════════════════════════

Check what will be committed:
  $ git ls-files src/plugins/external/
  Should only show __init__.py files

Install from private repo:
  $ pip install git+https://${TOKEN}@github.com/org/plugin.git@v1.0.0

Load plugin:
  $ export MAGNET_PLUGINS=magnet_plugins.plugin_name

Verify plugins loaded:
  $ python -c "from core.plugins.registry import PluginRegistry; \
               PluginRegistry.auto_load(); \
               print(PluginRegistry.list_available())"


═══════════════════════════════════════════════════════════════════════════════

For full details, see:
  📖 EXTERNAL_PLUGINS_STRATEGY.md
  📖 EXTERNAL_PLUGIN_QUICKSTART.md
  📖 EXTERNAL_PLUGINS_QUICK_REFERENCE.md

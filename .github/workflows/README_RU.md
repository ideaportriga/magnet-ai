# GitHub Actions Workflows

Этот документ описывает настроенные GitHub Actions workflows для проекта Magnet AI.

## 📋 Обзор Workflows

### 1. CI - Linting and Tests (`ci.yml`)

**Триггеры:**
- Push в ветки `main` и `develop`
- Pull requests в ветки `main` и `develop`
- Ручной запуск

**Что проверяет:**
- ✅ **API (Python)**
  - Ruff линтинг (проверка стиля кода)
  - Ruff форматирование
  - Pytest тесты с покрытием кода
  - Загрузка отчетов coverage в Codecov

- ✅ **Web (TypeScript/Vue)**
  - ESLint проверка (все проекты через Nx)
  - TypeScript проверка типов
  - Prettier форматирование
  - Тесты с покрытием кода

- ✅ **Docker**
  - Проверка сборки API образа
  - Проверка сборки Web образа
  - Кэширование слоев для ускорения

- ✅ **Security**
  - Проверка Python зависимостей на уязвимости (Safety)
  - Проверка npm/yarn зависимостей (yarn audit)

---

### 2. PR - Code Quality Checks (`pr-checks.yml`)

**Триггеры:**
- Открытие, обновление или повторное открытие Pull Request

**Особенности:**
- 🎯 **Умная оптимизация**: запускает проверки только для измененных файлов
- 🚀 **Параллельное выполнение**: независимые проверки выполняются одновременно
- 📊 **Автоматические комментарии**: добавляет комментарии с coverage в PR
- 🏷️ **Автоматические labels**: размечает PR по размеру (XS, S, M, L, XL)
- ⚡ **Отмена устаревших запусков**: автоматически отменяет старые проверки при новом push

**Проверки:**
- API: Ruff lint, Ruff format, Pytest
- Web: ESLint, TypeScript, Prettier, Tests
- PR Size Check (анализ размера изменений)

---

### 3. Auto Fix - Format & Lint (`auto-fix.yml`)

**Триггеры:**
- Ручной запуск из GitHub UI
- По расписанию (каждое воскресенье в 00:00 UTC)

**Что делает:**
- 🔧 Автоматически форматирует код (Ruff + Prettier)
- 🔧 Исправляет автоматически исправимые проблемы линтинга
- 📝 Создает коммит с изменениями
- 🚀 Пушит изменения в текущую ветку

**Использование:**
```bash
# Запустить вручную через GitHub UI:
Actions → Auto Fix - Format & Lint → Run workflow
```

---

### 4. Deploy VitePress Docs (`deploy-docs.yml`)

Автоматический деплой VitePress документации на GitHub Pages.

**Триггеры:**
- Push в `main` с изменениями в документации
- Ручной запуск

**Настройка:**

1. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в Settings → Pages
   - В разделе "Source" выберите "GitHub Actions"

2. **Настройте base URL (если нужно):**
   - Откройте `.github/workflows/deploy-docs.yml`
   - Измените переменную `HELP_BASE_URL` на нужное значение
   - По умолчанию используется `/magnet-ai/` (название репозитория)
   - Для кастомного домена можно установить `/` или оставить пустым

**Команды для локальной разработки:**
```bash
cd web
yarn nx run magnet-docs:dev       # Запуск dev сервера
yarn nx run magnet-docs:build     # Сборка документации
yarn nx run magnet-docs:preview   # Превью собранной документации
```

**После деплоя:**
Документация будет доступна по адресу `https://ideaportriga.github.io/magnet-ai/`

---

## 🚀 Быстрый старт

### Локальная разработка

**Python API:**
```bash
cd api
poetry install
poetry run ruff check . --fix        # Исправить проблемы линтинга
poetry run ruff format .             # Форматировать код
poetry run pytest                    # Запустить тесты
```

**Web:**
```bash
cd web
yarn install
yarn nx run-many --target=lint --all --fix    # ESLint fix
yarn prettier --write "**/*.{ts,tsx,js,jsx,vue,json,css,scss,md}"  # Prettier
yarn nx run-many --target=test --all         # Тесты
```

**Или используйте root команды:**
```bash
npm run lint        # Линтинг всего проекта
npm run lint:api    # Только API
npm run lint:web    # Только Web
```

---

## 🔧 Конфигурация

### Необходимые секреты (опционально)

Добавьте в `Settings → Secrets and variables → Actions`:

- `CODECOV_TOKEN` - для загрузки отчетов coverage в Codecov (опционально)

### Настройка branch protection rules

Рекомендуется добавить в `Settings → Branches → Branch protection rules` для `main`:

- ✅ Require a pull request before merging
- ✅ Require status checks to pass before merging
  - `API - Lint & Test`
  - `Web - Lint & Test`
  - `Docker - Build Check`
- ✅ Require branches to be up to date before merging
- ✅ Require conversation resolution before merging

---

## 📊 Мониторинг и отладка

### Просмотр логов
```
GitHub → Actions → Выбрать workflow → Выбрать run → Просмотреть логи
```

### Проверка кэша
```
GitHub → Actions → Caches
```

### Повторный запуск failed jobs
```
GitHub → Actions → Failed run → Re-run failed jobs
```

---

## 🎯 Best Practices

1. **Перед push**: запустите локальные линтеры
   ```bash
   npm run lint
   ```

2. **Коммиты**: используйте conventional commits
   ```
   feat: добавить новую функцию
   fix: исправить баг
   style: изменения форматирования
   refactor: рефакторинг кода
   test: добавить тесты
   docs: обновить документацию
   ```

3. **Pull Requests**: 
   - Держите PR маленькими (< 500 строк изменений)
   - Проверяйте, что все CI проверки прошли
   - Отвечайте на комментарии reviewers

4. **Используйте Auto Fix**: для быстрого исправления форматирования

---

## 🐛 Решение проблем

### CI падает на API линтинге
```bash
cd api
poetry run ruff check . --fix
poetry run ruff format .
git add -A
git commit -m "style: fix linting issues"
```

### CI падает на Web линтинге
```bash
cd web
yarn nx run-many --target=lint --all --fix
yarn prettier --write "**/*.{ts,tsx,js,jsx,vue,json,css,scss,md}"
git add -A
git commit -m "style: fix linting issues"
```

### Тесты падают локально, но не в CI (или наоборот)
- Проверьте версии зависимостей
- Очистите кэш: `poetry cache clear . --all` или `yarn cache clean`
- Переустановите зависимости

### Docker build падает
```bash
# API
docker build -t test-api ./api

# Web  
docker build -f Dockerfile -t test-web .
```

---

## 📈 Дальнейшие улучшения

Возможные дополнения:

- [ ] Интеграция с SonarQube/SonarCloud для анализа качества кода
- [ ] E2E тесты с Cypress/Playwright
- [ ] Performance тесты
- [ ] Автоматический deploy в staging при merge в develop
- [ ] Автоматический changelog генератор
- [ ] Dependabot для автоматического обновления зависимостей
- [ ] Automated security scanning (Snyk, GitHub Security)

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Ruff Documentation](https://docs.astral.sh/ruff/)
- [ESLint Documentation](https://eslint.org/)
- [Nx Documentation](https://nx.dev/)
- [Pytest Documentation](https://docs.pytest.org/)
